import {mean} from "../src/mean-array";

test("Esta es otra prueba, lindo", () => {

    expect(mean([2])).toBe(2);
})

test("Esta es otra prueba, lindo", () => {

    expect(mean([1])).toBe(1);
})

test("Esta es otra prueba, lindo", () => {

    expect(mean([3])).toBe(3);
})